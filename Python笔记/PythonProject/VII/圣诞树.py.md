# 圣诞树.py · 字符画圣诞树（随机装饰）

> **摘要**：使用随机生成的字母和数字作为装饰，打印一棵圣诞树字符画。树干为随机数字，树冠由随机字母组成，中心有一颗星（Φ）。

---

#### 💻 代码

```python
from random import *

def ranstr(x):
    s = ''
    n = int(x)
    for i in range(n):
        s += chr(int(randrange(ord('A'), ord('a') + 1)))
    return s

def ranstr1(x):
    s = ''
    n = int(x)
    for i in range(n):
        s += chr(int(randrange(ord('0'), ord('9') + 1)))
    return s

print('{:^21}'.format('Φ'), end='')
for i in range(0, 21, 2):
    print('{:^21}'.format(str(ranstr(i - 1))))
for i in range(4):
    print('{:^21}'.format(ranstr1(5)))
```

---

#### 📖 运行示例

```
           Φ
          Js
         HaeB
        HlbfiA
       fqAIgqFe
      zTtXflRWcZ
     sRoPZgELJNhb
    sBhhDwiUZHUSrO
   uSVhDcqeGTOqHnZf
  OtbLmwhiRGGhFeXVlH
    00000
    00000
    00000
    00000
```

---

#### 📐 代码解析

##### 树冠生成（`ranstr`）
- 每行字符数从 1 递增到 19（步长 2）
- 字符从 `'A'` 到 `'z'` 的 ASCII 范围内随机选择（包含中间的特殊字符）

##### 树干生成（`ranstr1`）
- 每行 5 个随机数字（0~9）
- 共 4 行

##### 树的形状
```
       Φ          ← 星（树顶）
       Js         ← 第1层（2个字符）
      HaeB        ← 第2层（4个字符）
     HlbfiA       ← 第3层（6个字符）
    fqAIgqFe      ← ...
   zTtXflRWcZ
  sRoPZgELJNhb
 sBhhDwiUZHUSrO
uSVhDcqeGTOqHnZf
OtbLmwhiRGGhFeXVlH  ← 第9层（18个字符）
    00000        ← 树干
    00000
    00000
    00000
```

---

#### 🔗 关联笔记
- [[循环结构#循环的嵌套]]
- [[random库的使用]]
- [[字符串#内置的字符串处理函数]]

#### 🔗 返回上级
- [[VII - 函数与递归进阶实验]]
